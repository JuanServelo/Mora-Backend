api:
  insecure: true
  dashboard: true

entryPoints:
  web:
    address: ":8087"

providers:
  consulCatalog:
    exposedByDefault: false
    endpoint:
      address: "consul:8500"

  consul:
    endpoint:
      address: "consul:8500"
    rootKey: "traefik/config"

log:
  level: INFO
